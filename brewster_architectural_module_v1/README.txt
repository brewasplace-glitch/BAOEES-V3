# BREWSTER Architectural Module v1 - updatepakket

Gebruik:

```powershell
cd "PAD_NAAR_JE_BAOEES_REPO"
powershell -ExecutionPolicy Bypass -File ".\install_architectural_module_v1.ps1"
```

Daarna committen:

```powershell
git add baoees/architectural docs/architectural outputs/architectural_examples
git commit -m "feat: add architectural module v1"
git push
git status
```
