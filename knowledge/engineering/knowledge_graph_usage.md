# Knowledge Graph Usage

Phoenix uses the Knowledge Graph to connect:

- project objects;
- requirements;
- standards;
- assumptions;
- calculations;
- documents;
- drawings;
- permits;
- decisions;
- source evidence.

Typical relations include `contains`, `documents`, `derived_from`,
`validated_by`, `requires`, `conflicts_with` and `supersedes`.
