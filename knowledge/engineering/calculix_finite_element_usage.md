# CalculiX Finite Element Usage

Set `CALCULIX_CCX` to the full `ccx` executable path when it is not on PATH.
Without CalculiX, BB14 remains operational in offline verification mode.
The offline solver is limited to one linear cantilever and is not a substitute
for full engineering verification.
