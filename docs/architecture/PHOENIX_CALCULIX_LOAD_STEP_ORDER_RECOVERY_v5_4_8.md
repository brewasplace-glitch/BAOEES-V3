# CalculiX Load Step Order Recovery v5.4.8

`*CLOAD` is moved inside `*STEP` and after `*STATIC`. The installed CalculiX 2.23-1 package, runtime PATH, `-i` argument, real C3D8 solve, DAT/FRD validation, detector, commit and push gates remain.
