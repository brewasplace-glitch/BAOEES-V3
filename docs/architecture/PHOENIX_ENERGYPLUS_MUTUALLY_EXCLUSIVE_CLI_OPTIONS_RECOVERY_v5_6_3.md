# EnergyPlus CLI Options Recovery v5.6.3

Removes `--accept-messages` because Qt Installer Framework marks it as mutually exclusive with `--default-answer`. Keeps `--accept-licenses`, `--default-answer`, `--confirm-command`, and `install`.
