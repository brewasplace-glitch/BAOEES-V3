# EnergyPlus Admin Elevation Recovery v5.6.4

Automatically restarts the same installer once with `-Verb RunAs`, preserves `-Repository`, uses `-PhoenixElevated` as loop guard, waits for the elevated child process, and propagates its exit code.
