# PROJECT PHOENIX — MOSKEE BUNSCHOTEN REAL-PROJECT E2E v1.0

Bound baseline: `project-phoenix` @ `99ba54c911fbb1e080a66961b6a5a5306c6bc0c7`.

Explicit project binding: `configs/projects/moskee_bunschoten.json` following **GO MOSKEE BUNSCHOTEN**.

Primary browser evidence: Playwright (Apache-2.0). Fallback: Selenium WebDriver (Apache-2.0). Third-party browser packages are installed in the isolated external venv `C:\PHOENIX-ENGINES\BrowserEvidence\venv`, never in Git. Existing Edge/Chrome is preferred; Playwright Chromium is installed only when no branded browser exists.

The real run uses `/start-v3/`, selects Moskee Bunschoten in AUTONOME PHOENIX-FLOW, starts the enabled projectflow through the UI, waits for terminal success, requires a recommendation, inventories project artifacts, browser-renders a real visual artifact with a non-blank screenshot gate, runs real CalculiX when a project-scoped `.inp` is available, and writes an external SHA-256 evidence manifest under `C:\PHOENIX-E2E-EVIDENCE\MOSKEE_BUNSCHOTEN`.

A BLOCKED result is evidence of the first real E2E blocker and is not converted into a false PASS. Production and FOR CONSTRUCTION remain locked.
