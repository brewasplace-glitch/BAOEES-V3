# ADR-0008 — CalculiX offline-safe adapter

Status: Accepted.

CalculiX is optional. Phoenix must still validate models, create input decks,
run deterministic verification and preserve evidence when `ccx` is absent.
