# PEK Factory v1.0

Generates deterministic Python scaffolding from EKMS.

```powershell
python -m engineering_kernel.factory --domain MATH --limit 10 --dry-run
```

The generator never invents engineering formulas and never overwrites files unless explicitly requested.
